<Info>
  **Working with Amounts**: All µUSDC amounts use TypeScript's `bigint` type to ensure precision. The SDK provides helper functions like `usdcToUusdc()`, `uusdcToUsdc()`, and `formatUusdcDisplay()` to convert between USDC and µUSDC.
</Info>

